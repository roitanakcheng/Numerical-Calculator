# Numerical-Calculator
A simple terminal-based calculator that can be used for chained calculations. It is still lacking in security measures since i am only using the eval function to calculate numbers input, but it is usable.
